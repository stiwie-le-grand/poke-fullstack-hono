import { createPartialApp } from "../../lib/createApp";
import { {{toPascalCase routeName}}Controller } from "./{{routeName}}.controller";
import * as routes from "./{{routeName}}.routes";
const router = createPartialApp();

const {{routeName}}Controller = new {{toPascalCase routeName}}Controller();

router.openapi(routes.listUnauthenticated, {{routeName}}Controller.list{{routeName}});

export const {{routeName}}Router = router;
